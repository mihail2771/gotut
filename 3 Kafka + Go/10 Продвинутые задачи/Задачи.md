1. Event-driven сервис
Producer: создаёт Order
Consumer:
обрабатывает заказ
пишет результат в другой топик
🔹 2. Saga (упрощённая)
несколько шагов:
create order
reserve balance
confirm
при ошибке → откат
🔹 3. Логирование событий
все события пишутся в Kafka
отдельный consumer:
пишет в файл / БД
🔹 4. Rate limiting consumer
ограничь:
N сообщений в секунду
🔹 5. Streaming pipeline
producer → kafka → consumer → обработка → kafka → другой consumer